import 'package:flutter/material.dart';

/// ini contoh
class ReaderPage extends StatelessWidget {
  const ReaderPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: const Text("Reader"),
        ),
        body: const Text("Reader!"));
  }
}
